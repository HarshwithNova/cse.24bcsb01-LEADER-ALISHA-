// const params = new URLSearchParams(window.location.search);
// const id = params.get("id");

// async function loadDetail() {
//   const res = await fetch(`/api/asteroids/${id}`);
//   const a = await res.json();

//   document.getElementById("details").innerHTML = `
//     <h2>${a.name}</h2>
//     <p><b>Hazardous:</b> ${a.hazardous}</p>
//     <p><b>Diameter (km):</b> ${a.diameter}</p>
//     <p><b>Velocity:</b> ${a.velocity} km/h</p>
//     <p><b>Miss Distance:</b> ${a.missDistance} km</p>
//     <p><b>Close Approach:</b> ${a.approachDate}</p>
//   `;
// }

// loadDetail();
